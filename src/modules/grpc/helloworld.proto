syntax = "proto3"

package helloworld;

service Greeter {
    // 基础daemon
    rpc SayHello (HelloRequest) returns (HElloResponse) {}
}

// 简单请求
message HelloRequest {
    string name = 1;
}

// 简单响应
message HelloResponse {
    string message = 1;
}

